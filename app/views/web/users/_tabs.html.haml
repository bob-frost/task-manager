%ul.nav.nav-tabs
  %li{class: ('active' if active == :users)}
    = link_to t('.profile'), resource_user
  %li{class: ('active' if active == :tasks)}
    = link_to t('.tasks'), user_tasks_path(resource_user)
